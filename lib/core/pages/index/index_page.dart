import 'package:flutter/material.dart';

import 'index_view.dart';

class IndexPage extends StatelessWidget {
  const IndexPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // no blocprovider for now
    return IndexView();
  }
}
